[pytest]
markers =
    integration: marks tests requiring GPU + model checkpoints (deselect with -m "not integration")
testpaths = tests
addopts = --junitxml=outputs/test/last_run.xml --json-report --json-report-file=outputs/test/last_run.json --json-report-indent=2
